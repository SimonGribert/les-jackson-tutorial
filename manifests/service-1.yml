apiVersion: v1
kind: Service
metadata:
    name: "ljservices-4938"
    labels:
        app: "ljservices-4938"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "ljservices-4938"