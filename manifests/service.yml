apiVersion: v1
kind: Service
metadata:
    name: "ljservices-db88"
    labels:
        app: "ljservices-db88"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "ljservices-db88"